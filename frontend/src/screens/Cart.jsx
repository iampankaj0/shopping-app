import React from "react";

const Cart = () => {
  const compStyle = {
    height: "70vh",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  };

  return (
    <div style={compStyle}>
      <h1>Cart</h1>
    </div>
  );
};

export default Cart;
