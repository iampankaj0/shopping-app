import React from "react";

const Login = () => {
  const compStyle = {
    height: "70vh",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  };
  return (
    <div style={compStyle}>
      <h1>Login</h1>
    </div>
  );
};

export default Login;
